export class Segments {
    segment_id: number; 
    segment: string;    
  
    constructor(segment_id: number, segment: string) {
      this.segment_id = segment_id;
      this.segment = segment;
    }
}
