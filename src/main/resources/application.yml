spring:
  application:
    name: CarFusion
  datasource:
    url: jdbc:postgresql://localhost:5432/CarFusion1
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
